import { ContentListPipe } from './content-list.pipe';

describe('ContentListPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentListPipe();
    expect(pipe).toBeTruthy();
  });
});
